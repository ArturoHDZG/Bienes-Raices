<?php

// $passwordHash = password_hash($password, PASSWORD_BCRYPT);

// Create user
// $query = "INSERT INTO vendors (name, lastname, phone, email, password) VALUES ('{$name}', '{$lastname}', '{$phone}', '{$email}', '{$passwordHash}')";

// Update vendor
// $id = '1';
// $query = "UPDATE vendors SET email = '{$email}', password = '{$passwordHash}' WHERE id = {$id}";

// Update data
// mysqli_query($db, $query);

// Session data
// if ($auth) {
//   session_start();

//   $_SESSION['name'] = $vendor['name'];
//   $_SESSION['lastname'] = $vendor['lastname'];
//   $_SESSION['phone'] = $vendor['phone'];
//   $_SESSION['email'] = $vendor['email'];
//   $_SESSION['date'] = $vendor['date'];
//   $_SESSION['login'] = true;
// }
